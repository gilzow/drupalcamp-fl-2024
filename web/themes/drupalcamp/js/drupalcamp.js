/**
 * @file
 * drupalcamp behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.drupalcamp = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
